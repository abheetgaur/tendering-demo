document.getElementById("registration-form").addEventListener("submit", function(event) {
  event.preventDefault(); // Prevent the form from submitting for demo purposes
  alert("Bidder registration submitted successfully!");
});
